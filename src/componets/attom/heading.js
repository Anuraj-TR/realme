
export const SubHeading = ({children}) => {
    return(
        <div className="rlm-title-header">
            <h2 className="rlm-subtitle">
                {children}
            </h2>
        </div>
    )
}